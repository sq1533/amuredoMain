# amuredoMain 프로젝트 구현 계획 (요구사항 종합 총정리)

본 문서는 프로젝트 **amuredoMain**에 대해 지금까지 논의된 모든 아키텍처, UI/UX 규격, 그리고 프론트엔드/백엔드 로직 요구사항을 빠짐없이 취합해 정리한 종합 구현 계획서입니다.

## 1. 프로젝트 개요
- **프로젝트 명:** amuredoMain
- **디자인 철학:** 모바일 기기 환경을 메인(Mobile-First)으로 설정하는 반응형 웹 서비스 구성
- **글로벌 레이아웃 제한:** 모든 페이지의 표시 폭은 **최대 `1024px`**까지로 한계가 제한되며, 이보다 넓은 와이드 스크린 접속 시 웹페이지의 콘텐츠가 화면 중앙에 고정(가운데 정렬)되도록 규칙을 지정합니다.

## 2. 개발 스택 (Technology Stack)
- **프론트엔드 (Frontend):** HTML, CSS (순수 바닐라 디자인 룰 적용), JavaScript
- **백엔드 (Backend):** FastAPI (Python 웹 프레임워크 활용)
- **데이터 활용 (DB):** Firebase Store (Firestore 연동을 통한 데이터 읽기 서비스)
- **외부 연동 (API):** Telegram Bot API 등 활용 모색

## 3. 백엔드 핵심 기능 명세
FastAPI 서버는 다음의 라우팅과 비즈니스 로직 역할을 수행합니다.
1. **프론트엔드 페이지 활성화 (서빙):** 구축된 정적 웹 페이지들을 로드하여 반환 지원.
2. **페이지 스위치(Switch) 연결:** 네비게이션을 통한 상세 페이지 전환 시, 알맞은 HTML 리소스로 라우팅 처리.
3. **Firebase Store 데이터 읽기 연결:** 프론트엔드의 화면 렌더링에 사용할 데이터 호출에 대응하여, Firebase 데이터베이스 내역(배너 리스트 등)을 읽어 클라이언트로 전달해주는 중간 다리 수행.
4. **Telegram 구동 메세지 발송:** 웹페이지에서 유저가 발생시킨 특정 액션(버튼 클릭/폼 이벤트 등)을 트리거로 삼아, 서버에서 지정된 텔레그램 메세지를 봇 API로 발송하는 구조 확립.

## 4. 프론트엔드 세부 디자인 및 UI 규격
모든 접속 환경에서, 페이지는 상층부부터 콘텐츠가 일자로 배치되는 전형적인 **Top-Down 구조**를 채택합니다.

### 4.1. 최상단 영역 (Header Layout)
웹페이지 제일 꼭대기는 가로 묶음으로 `[1. 로고 이미지] + [2. 여백 유지(Empty)] + [3. 페이지 네비게이션]` 구성을 이룹니다.

- **1) 로고 이미지 구성 (Logo)**
  - **가로길이(Width):** `1024px`의 메인 컨테이너 너비 기준으로 **`1/4 (25%)`**에 달하는 면적을 차지해야 합니다.
  - **세로길이(Height):** 로고의 세로 사이즈는, 위 가로 너비의 **`1/2 (50%)`** 크기 분량을 항상 유지해야 합니다 (종횡비 가로 2 : 세로 1 고정).
- **2) 빈 공간 (Empty Space)**
  - 중앙의 빈 영역은 별도의 요소 구성 없이 좌(로고)/우(메뉴)를 자연스럽게 밀어내는 레이아웃을 통해 빈 공간으로 설정합니다.
- **3) 동적 반응형 네비게이션 메뉴 (Responsive Navigation)**
  - 접속 기기 화면의 실시간 가로 픽셀(`640px`)을 분기점으로 삼아 메뉴 형태가 다르게 구성됩니다.
  - **640px 초과 (PC/와이드):** 네비게이션의 페이지 스위치 기능 및 버튼들이 헤더 우측에 **가로로 전개되어 펼쳐진(Expanded)** 형태로 즉시 노출됩니다.
  - **640px 이하 (모바일):** 공간 제약을 위해 메뉴 버튼들은 전부 묶여 숨겨집니다. 해당 메뉴 창을 직접 호출할 수 있는 **정사각형의 버튼(햄버거 토글러 등)**만이 노출됩니다.
  - **모바일 토스트 슬라이딩 로직:** 사용자가 메뉴 정사각형 버튼을 클릭하면, 화면의 좌측 바깥에서 메뉴를 담고 있는 인터페이스가 측면 팝업창(**왼쪽 토스트/드로어 팝업**) 형태로 슬라이드되어 나타납니다. 나타난 메뉴에는 페이지 Switch 기능 버튼들이 들어있습니다.

### 4.2. 메인 화면: 배너 영역 (Main Carousel Banner)
최상단 로고/메뉴 헤더의 바로 아랫부분부터 메인 배너가 자리 잡습니다.

- **1) 크기 비율 및 꽉 찬 레이아웃**
  - **너비(Width):** 어떠한 접속 기기이든 허용된 상자(최대 1024px 제한) 안에서 **가로 범위를 100% 빈틈없이 꽉 채우도록 설정(Full Size)**.
  - **높이(Height):** 그에 맞춰 표시되는 배너의 로드된 너비 **`60% 비율 값`**의 세로 높이에 언제나 고정됩니다 (즉, 종횡비 가로 5 : 세로 3 기준치 정립).
- **2) Firebase 연동 데이터 주입 규격**
  - Firebase Store에 세팅될 `vanner > vanner > paths(list)` 데이터 트리 구조에 위치한 URL 목록들을 가져옵니다.
  - 리스트 안에 담긴 각각의 URL들을 모두 순서대로 불러와, 프론트엔드의 화면에 배너 이미지로서 <img> 태그에 일괄 삽입하여야 합니다.
- **3) 캐러셀 애니메이션 및 전환 동기화**
  - 삽입된 이미지가 여러 장이어도 고객의 모니터에는 한 번에 1장의 이미지만이 노출됩니다.
  - **5초 자동 슬라이드:** 특정 이미지가 화면에 표시된 채로 **5초 이상** 지나게 되면 다음 이미지의 순서로 애니메이션이 작동하며 자동으로 넘어갑니다.
  - **개입에 의한 전환:** 5초를 채우지 않아도 사용자가 **'이전(Prev)', '다음(Next)' 버튼을 클릭**하는 액션을 취하면 즉각적으로 해당 넘김 화면으로 전환되며, 동시에 타이머도 재시작되어야 합니다.
- **4) '이전', '다음' 컨트롤러 모양 및 Hover 설계**
  - **호버링(Hover) 등장 연출:** 기본적으로 배너 위에 어떠한 버튼도 보이지 않게 투명 숨김(Opacity 0) 처리하다가, 사용자가 배너 영역 내에 **마우스 포인터를 올려놓았을(Hover)** 때에만 두 개의 제어 컨트롤러 버튼이 시각적으로 나타납니다.
  - **위치 제한:** 배너 상자 안에서 제일 바깥 왼쪽 구석(이전 버튼), 제일 바깥 오른쪽 구석(다음 버튼) 모서리에 부착됩니다.
  - **사이즈 기준선:** 컨트롤러 버튼의 높이는 사진과 **완벽히 동일한 배너 100%의 높이**를 가지게 하며, 너비는 어떠한 상태에서도 **`10px 고정 두께`**를 갖도록 디자인해야 합니다.

### 4.3. 메인 화면: 베스트 아이템 카드 영역 (Item Cards)
메인 배너의 바로 아래에는 추천 상품 목록을 나열하는 기능 섹션이 이어집니다.

- **1) 서브 타이틀 (Subtitle)**
  - 상품 진열 구역 상단에 **'Best'** 라는 섹션 타이틀 문구를 표시합니다.
- **2) 아이템 갤러리 레이아웃 (Grid)**
  - 메인 컨테이너 레이아웃 내부에서 가로 **고정 3열 (3 Columns)** 배치를 갖는 그리드(Grid) 형식으로 화면을 균등 분할합니다.
  - 아이템의 총 개수에 따라 아래로 새 줄이 생기며 세로 행(Row)은 다르게(유동적으로) 연장되도록 구성합니다.
- **3) 데이터 연동 조건 (Firebase Store 필터링)**
  - 데이터베이스의 `items > [id]` 구조를 조회하여 상품 목록을 확인합니다.
  - 이 중 요소 객체의 값이 **`event : 'best'`** 에 해당하는 아이템 요소들만 특별히 골라내어 이번 카드 영역에 배정합니다.
- **4) 개별 카드(Item Card) 내부 구성 비율**
  - **대표 이미지:** 카드 영역 구성 시, 최상단에 놓일 상품 사진은 강제로 **가로:세로 `1:1 (정사각형)` 비율**을 유지하도록 시각적으로 고정시킵니다.
  - **상품명 (Name):** 이미지 하단에 문자열(string) 형태로 기재됩니다.
  - **상품 가격 (Price):** 상품명 아래 위치에 상품 가격이 텍스트(string) 형태로 노출됩니다.
- **5) 클릭 이벤트 동작 연계 (Page Switch)**
  - 개별 카드의 **'상품 대표 이미지'** 부분, 또는 **'상품명(Name) 텍스트'** 영역을 클릭했을 때 이벤트를 감지시킵니다.
  - 클릭이 감지되면 FastAPI 백엔드 라우터를 거쳐 해당 제품의 구체적인 **상세 정보 페이지로 전환(Page Switch)** 되도록 링크를 라우팅할 예정입니다.
