# amuredoMain 프로젝트 구현 계획

본 문서는 사용자의 초기 요구사항을 바탕으로 작성된 웹 어플리케이션 아키텍처 및 화면 구성에 대한 기본 구축 계획입니다.

## 1. 프로젝트 개요
- **목표:** 모바일 환경 우선(Mobile-First) 반응형 웹 기반 홈페이지 구축
- **레이아웃 제한:** 모든 페이지의 최대 너비(Max-width)는 `1024px`로 제한하여 와이드 스크린에서도 레이아웃이 깨지지 않게 화면 중앙에 배치.

## 2. 기술 스택 (Tech Stack)
- **프론트엔드:** HTML, CSS (Vanilla CSS), JavaScript
- **백엔드:** FastAPI (Python 기반 고성능 웹 프레임워크)
- **데이터베이스/서비스 연동:** Firebase (주요 기능: Firestore 등의 데이터 읽기)
- **외부 API 연동:** Telegram Bot API (주요 기능: 특정 이벤트 발생 시 텔레그램 메세지 전송)

## 3. 백엔드 주요 기능 (FastAPI)
- **프론트엔드 자원 서빙:** 정적 파일(HTML, CSS, JS) 서빙 및 페이지 활성화
- **페이지 전환(Switch) 라우팅:** 프론트엔드의 화면 전환 요청에 대응하는 URL 및 데이터 라우팅
- **Firebase 연동 API:** Firebase Store 데이터베이스에 접근하여 데이터를 읽어오는 보안된 API 엔드포인트 마련
- **텔레그램 알림 API:** 사용자 행동(예: 폼 제출, 특정 버튼 클릭 등) 트리거에 따라 설정된 텔레그램 봇을 통해 특정 채팅방으로 메시지를 전송하는 로직 구현

## 4. 프론트엔드 메인 페이지 구성 & 레이아웃 세부안
최상단부터 콘텐츠가 이어지는 **Top-down** 구조를 가집니다.

### 4.1. 최상단 헤더(Header) 영역
헤더는 `로고 이미지` + `여백(Empty)` + `페이지 네비게이션` 요소가 양옆으로 배치되는 구조입니다 (예: Flexbox의 space-between 활용).

### 4.2. UI 컴포넌트 상세 규격
#### **1) 로고 이미지**
- **가로 길이(Width):** 메인페이지(컨테이너) 전체 가로 너비의 **1/4 (25%)**
- **세로 길이(Height):** 로고 이미지 가로 길이의 **1/2 (50%)** 
  *(가로:세로 종횡비를 2:1 로 유지하여 반응형으로 자연스럽게 축소/확대됨)*

#### **2) 페이지 네비게이션 (Page Navigation)**
화면 가로 해상도 `640px`을 기준으로 디자인과 상호작용이 변경되는 반응형 구조입니다.

| 구분 | 화면 크기 (Width) | 동작 및 디자인 사양 |
| :--- | :--- | :--- |
| **PC / 태블릿** | `640px` 초과 | 네비게이션 메뉴가 **펼쳐진 형태(Expanded)**로 표시됨.<br>페이지를 전환할 수 있는 Switch 버튼들이 헤더 우측에 나란히 노출. |
| **모바일** | `640px` 이하 | 공간 확보를 위해 메뉴가 숨겨지고 **정사각형 모양의 버튼(예: 햄버거 메뉴)**이 우측 노출.<br>- **클릭 시 액션:** 화면 왼쪽에서 오른쪽으로 나타나는 측면 팝업창(**왼쪽 토스트/드로어 팝업**) 형태로 페이지 Switch 버튼들이 슬라이드되어 노출됨. |

## 5. 확인 필요 사항 (User Review Required)

> [!IMPORTANT]
> **추가 요구사항 관련**
> 1. 위 정리된 기본 구조 외에 상세 페이지에 들어가야 할 내용이나, 메인 페이지의 메인 콘텐츠 영역(헤더 아래 영역)에 필요한 요소가 있다면 말씀해주세요.
> 2. 디자인 컬러맵(메인 색상 등), 로고 이미지(디자인용 샘플이 필요한지 여부), Firebase에서 구체적으로 어떤 구조의 데이터를 읽어오는지, 텔레그램 메세지가 전송되어야 하는 트리거 조건 등에 대해 구상하신 부분이 있다면 공유해주세요.

사용자님의 추가 요청사항을 전달해주시면, 해당 내용을 바탕으로 계획을 업데이트하고 실제 코드 작성 단계로 넘어가도록 하겠습니다.
